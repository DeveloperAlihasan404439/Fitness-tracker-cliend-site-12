import useClass from "../Sheard/Hooks/useClass";

const Classsiex = () => {
    const {classAll,isLoading,refetch} = useClass()
    const classItems = classAll.filter(classItems =>)
    return (
        <div>
            
        </div>
    );
};

export default Classsiex;